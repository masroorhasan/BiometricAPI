module.exports = {
    getUser: function(req) {
        /**
         * Return the unique username of the logged in
         * user given the request object
         */
        return "masroor";
    }
};
